import styled from "styled-components";

const StyledFooter = styled.div`
  display: flex;
  flex-direction: row;
  justify-content: space-between;
  padding-block: 36px 35px;
`;

const Copyrite = styled.p`
  margin: 0;
`;
const Legacy = styled.p`
  margin: 0;
`;

export { StyledFooter, Copyrite, Legacy };
